<div id="home-container" class="container">
  <h2>Add a Flatname to get started</h2>
  <p>Be creative! We won't  judge 😉</p>
  <div class="card no-shadow">
    <div class="card-body">
      <h2>Create your flat</h2>
      <%= simple_form_for @flat do |f| %>
        <%= f.input :name ,label: "Flat Name", error: "Please enter a flat name" %>
        <%= f.button :submit, class: "btn-7" %>
      <% end %>
    </div>
  </div>
</div>