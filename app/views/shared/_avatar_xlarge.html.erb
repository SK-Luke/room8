<% if user.photo.attached? %>
  <%= cl_image_tag(user.photo.key, transformation: [{ height: 500, width: 500, crop: "thumb", gravity: "face"}], class: "avatar-xlarge") %>
<% else %>
  <%= image_tag "anonavatar.png", class: "avatar-xlarge" %>
<% end %>
