<% if current_user.photo.attached? %>
  <%= cl_image_tag(current_user.photo.key, transformation: [{ height: 100, width: 100, crop: "thumb", gravity: "face"}], class: "avatar") %>
<% else %>
  <%= image_tag "anonavatar", height: "100px", width: "100px", class: "avatar" %>
<% end %>
